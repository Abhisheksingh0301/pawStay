<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Create Pet Sitter Profile</title>
  <link href="/tailwind/output.css" rel="stylesheet">
  <link rel="icon" sizes="16x16" href="/images/pawstay-logo.png">
</head>

<body class="bg-gray-100 min-h-screen flex flex-col">

  <%- include('header', { user }) %>

  <!-- PAGE TITLE -->
  <div class="bg-blue-600 text-white py-4">
    <div class="max-w-7xl mx-auto px-6">
      <h1 class="text-2xl font-bold">Create Pet Sitter Profile</h1>
      <p class="text-sm mt-1">User ID: <%= user.user_id %></p>
    </div>
  </div>

  <!-- MAIN CONTENT (fills remaining screen) -->
  <main class="flex-1">
    <div class="grid grid-cols-1 lg:grid-cols-2 h-full" style="background-image: url('/images/pet-sitter.jpg'); background-size: cover; background-position: center;"
      >

      <!-- LEFT : FULL HEIGHT BACKGROUND -->
      <div
        class="hidden lg:block relative h-full">
        <!-- <div class="absolute inset-0 bg-black bg-opacity-40"></div> -->

        <div class="relative z-10 h-full flex items-end p-10 text-white">
          <div>
            <h2 class="text-3xl font-bold text-cyan-300">Become a Pet Sitter</h2>
            <p class="mt-2 text-cyan-200">
              Share your experience and connect with pet owners
            </p>
          </div>
        </div>
      </div>

      <!-- RIGHT : FORM (centered vertically) -->
      <div class="flex items-center justify-center bg-gray-200 px-6 opacity-70">
        <div class="bg-white rounded-xl shadow-lg p-8 w-full max-w-xl">

          <form
            action="/pet_sitter/create-profile"
            method="POST"
            enctype="multipart/form-data"
            class="space-y-6"
          >

            <input type="hidden" name="user_id" value="<%= user.user_id %>">

            <!-- BIO -->
            <div>
              <label class="block font-semibold text-gray-700 mb-2">Bio</label>
              <textarea name="bio" rows="4" required
                class="w-full border rounded px-3 py-2 focus:ring focus:ring-green-300 focus:outline-none"></textarea>
            </div>

            <!-- CERTIFICATIONS -->
            <div>
              <label class="block font-semibold text-gray-700 mb-2">Certifications</label>
              <input type="text" name="certifications" placeholder="Like Vet Assistant"
                class="w-full border rounded px-3 py-2 focus:ring focus:ring-green-300 focus:outline-none">
            </div>

            <!-- EXPERIENCE -->
            <div>
              <label class="block font-semibold text-gray-700 mb-2">Years of Experience</label>
              <input type="number" name="years_of_experience" min="0" required
                class="w-full border rounded px-3 py-2 focus:ring focus:ring-green-300 focus:outline-none">
            </div>

            <!-- SERVICES -->
            <div>
              <label class="block font-semibold text-gray-700 mb-2">Services Offered (Use Ctrl+Click to select multiple)    </label>
              <select name="services_offered[]" multiple required
                class="w-full h-40 border rounded px-3 py-2 focus:ring focus:ring-green-300 focus:outline-none">
                <option value="Dog_walking">Dog Walking</option>
                <option value="Pet_boarding">Pet Boarding</option>
                <option value="Pet_daycare">Pet Daycare</option>
                <option value="Overnight_care">Overnight Care</option>
                <option value="Drop_in_visits">Drop-in Visits</option>
                <option value="Special_needs_care">Special Needs Care</option>
                <option value="Emergency_and_vet_visits">Emergency & Vet Visits</option>
              </select>
            </div>

            <!-- PRICE -->
            <div>
              <label class="block font-semibold text-gray-700 mb-2">Price per Service ($)</label>
              <input type="number" name="price_per_service" min="0" step="0.01" required
                class="w-full border rounded px-3 py-2 focus:ring focus:ring-green-300 focus:outline-none">
            </div>

            <!-- PROFILE PICTURE -->
            <div>
              <label class="block font-semibold text-gray-700 mb-2">Profile Picture</label>
              <input type="file" name="profile_picture" accept="image/*">
            </div>

            <!-- ACTIONS -->
            <div class="flex justify-end gap-4 pt-4">
              <a href="/pet_sitter_dashboard"
                class="px-5 py-2 border rounded text-gray-700 hover:bg-gray-100">
                Cancel
              </a>
              <button type="submit"
                class="bg-green-600 text-white px-6 py-2 rounded hover:bg-green-700 transition">
                Save Profile
              </button>
            </div>

          </form>

        </div>
      </div>

    </div>
  </main>

</body>
</html>
