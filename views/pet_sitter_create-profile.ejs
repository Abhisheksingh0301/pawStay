<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create Pet Sitter Profile</title>
  <link href="/tailwind/output.css" rel="stylesheet">
  <link rel="icon" sizes="16x16" href="/images/pawstay-logo.png">
</head>

<body class="min-h-screen bg-no-repeat bg-center bg-size[800px_600px]" style="background-image: url('/images/puppy2.jpg');">

  <%- include('header', { user }) %>

  <!-- TOP INFO RIBBON -->
  <div class="relative bg-linear-to-r from-blue-600 to-cyan-500 text-white">
    <div class="max-w-7xl mx-auto px-6 py-8">
      <h1 class="text-3xl font-bold">Become a Pet Sitter 🐾</h1>
      <p class="mt-2 text-blue-100 max-w-4xl">
        Share your experience, offer professional pet care services, and connect
        with pet owners looking for trusted sitters.
      </p>
    </div>
  </div>

  <!-- MAIN CONTENT -->
  <main class="max-w-7xl mx-auto px-6 py-10">

    <!-- FORM CARD -->
    <div class="bg-white/80 rounded-2xl shadow-xl p-8 md:p-10">

      <form
        action="/pet_sitter/create-profile"
        method="POST"
        enctype="multipart/form-data"
        class="space-y-8"
      >

        <input type="hidden" name="user_id" value="<%= user.user_id %>">

        <!-- BIO (FULL WIDTH ALWAYS) -->
        <div class="opacity-80">
          <label class="block font-semibold text-gray-700 mb-2">Bio</label>
          <textarea
            name="bio"
            rows="4"
            required
            placeholder="Tell pet owners about yourself..."
            class="w-full border rounded-lg px-4 py-3 focus:ring-2 focus:ring-green-400 focus:outline-none"
          ></textarea>
        </div>

        <!-- TWO COLUMN GRID (RESPONSIVE) -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">

          <!-- CERTIFICATIONS -->
          <div>
            <label class="block font-semibold text-gray-700 mb-2">Certifications</label>
            <input
              type="text"
              name="certifications"
              placeholder="Vet Assistant, Groomer, etc."
              class="w-full border rounded-lg px-4 py-3 focus:ring-2 focus:ring-green-400 focus:outline-none"
            >
          </div>

          <!-- EXPERIENCE -->
          <div>
            <label class="block font-semibold text-gray-700 mb-2">Years of Experience</label>
            <input
              type="number"
              name="years_of_experience"
              min="0"
              required
              class="w-full border rounded-lg px-4 py-3 focus:ring-2 focus:ring-green-400 focus:outline-none"
            >
          </div>

          <!-- PET CATEGORY -->
          <div>
            <label class="block font-semibold text-gray-700 mb-2">Types of Pets</label>
            <input
              type="text"
              name="pet_category"
              placeholder="Dogs, Cats, Birds, Rabbits..."
              required
              class="w-full border rounded-lg px-4 py-3 focus:ring-2 focus:ring-green-400 focus:outline-none"
            >
          </div>

          <!-- PRICE -->
          <div>
            <label class="block font-semibold text-gray-700 mb-2">Price per Service ($)</label>
            <input
              type="number"
              name="price_per_service"
              min="0"
              step="0.01"
              required
              class="w-full border rounded-lg px-4 py-3 focus:ring-2 focus:ring-green-400 focus:outline-none"
            >
          </div>

        </div>

        <!-- SERVICES (FULL WIDTH) -->
        <div>
          <label class="block font-semibold text-gray-700 mb-2">
            Services Offered
            <span class="text-sm text-gray-500">(Ctrl / Cmd + Click to select multiple)</span>
          </label>
          <select
            name="services_offered[]"
            multiple
            required
            class="w-full h-40 border rounded-lg px-4 py-3 focus:ring-2 focus:ring-green-400 focus:outline-none"
          >
            <option value="Dog_walking">Dog Walking</option>
            <option value="Pet_boarding">Pet Boarding</option>
            <option value="Pet_daycare">Pet Daycare</option>
            <option value="Overnight_care">Overnight Care</option>
            <option value="Drop_in_visits">Drop-in Visits</option>
            <option value="Special_needs_care">Special Needs Care</option>
            <option value="Emergency_and_vet_visits">Emergency & Vet Visits</option>
          </select>
        </div>

        <!-- PROFILE PICTURE (FULL WIDTH) -->
        <div>
          <label class="block font-semibold text-gray-700 mb-2">Profile Picture</label>
          <input
            type="file"
            name="profile_picture"
            accept="image/*"
            class="block w-full text-sm text-gray-600"
          >
        </div>

        <!-- ACTIONS -->
        <div class="flex justify-end gap-4 pt-6 border-t">
          <a
            href="/pet_sitter_dashboard"
            class="px-6 py-2 rounded-lg border text-gray-700 hover:bg-gray-100 transition"
          >
            Cancel
          </a>
          <button
            type="submit"
            class="bg-green-600 text-white px-8 py-2 rounded-lg hover:bg-green-700 transition shadow"
          >
            Save Profile
          </button>
        </div>

      </form>
    </div>
  </main>

</body>
</html>
